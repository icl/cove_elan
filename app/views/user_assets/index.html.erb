<h1>Listing documents</h1>

<table>
  <tr>
    <th>File Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_assets.each do |asset| %>
  <tr>
    <td><%= File.basename(asset.file_name.to_s) %></td>
    <td><%= link_to 'Edit', edit_user_asset_path(asset) %></td>
    <td><%= link_to 'Destroy', asset, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User Asset', new_user_asset_path %>
