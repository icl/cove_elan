<%= simple_form_for @user_asset, :html => {:multipart => true} do |f| %>
  <% if @user_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_asset.errors.count, "error") %> prohibited this user_asset from being saved:</h2>

      <ul>
      <% @user_asset.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <%= f.association :meta_data_groups %>
  </div>

  <div class="field">
    <%= f.file_field :file_name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
