<%= simple_form_for @template,
   :url => url_for(:action => 'update_meta_data', :controller => 'templates'),
   :method => 'post' do |f| %>
  <% if @template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@template.errors.count, "error") %> prohibited this template from being saved:</h2>

      <ul>
      <% @template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="span10">
    <%= render :partial => 'meta_data_groups/groups_form', :locals => { :f => f, :object => @template } %>
  </div>

  <div class="span10">
    <%= f.button :submit %>
  </div>

<% end %>
