  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">

        <%= link_to "Cove", root_path, :class => 'brand'  %>
      <ul class="nav">
        <li>
          <%= link_to "Projects", projects_path %>
        </li>
        <li>
          <%= link_to "Corpora", corpora_path %>
        </li>
      </ul>

      <ul id="user-menu" class="nav pull-right">
        <% if user_signed_in? %>
            <li>
              <%= gravatar_for current_user.email, :size => 20 %>
              </li>
              <li>
              <%= link_to current_user.email, edit_user_password_path(current_user) %>
              </li>
          <li>
            <%= link_to('Sign out', destroy_user_session_path, :method => :delete) %>
            </li>

         <% else %>
          <li>
            <%= link_to('Sign in', new_user_session_path)  %>
          </li>
          <% end %>
        </li>
      </ul>
      </div>
    </div>
  </div>

