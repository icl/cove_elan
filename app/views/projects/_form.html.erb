<%= simple_form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <br /><br />

  <div class="field">
    <%= f.input :name %><br />
  </div>

  <div class="field">
    <%= f.association :templates, :collection => ::Template.all(:order => 'name'), 
      :label_method => lambda { |x| x.name_or_id } %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
